# 튜터 세션 인수인계

> **다음 세션은 이 파일부터 읽는다.** 학습(강의노트·과제) 작업의 현재 상태와 다음 할 일을 담는다.
> 상태는 자주 바뀌므로 작업이 끝날 때마다 갱신한다. 규칙 정본은 `강의기록-가이드.md`, 사고 기록은 `FIXLOG-강의노트.md`.
> 최종 갱신 **2026-08-28**

---

## 1. 지금 어디까지 왔나

| 구간 | 노드 | 강 | 상태 |
|---|---|---|---|
| 적응기 | adn1~5 | 24강 | ✅ 완료 |
| 모듈1 | m1n1~7 | 53강 | ✅ 완료 |
| 모듈2 | m2n1~9 | 75강 | ✅ 완료 |
| 모듈3 | **m3n1~4** | 37강 | ✅ 열린 만큼 완료 |
| **합계** | **25노드** | **189강** | 허브 기준 8주차 |

- 저장소 HEAD: **`3acd181`** (2026-08-28 pull 완료·미커밋 0)
- 강의노트 정본: `00_log/강의노트-모두연.html`

## 2. 바로 다음에 할 일

1. **모듈3 노드5가 열리면 기록** — LMS 코스 `2171`(모듈3). 노드3=`5159/26025~`, 노드4=`5160/26034~`였다. step URL은 순차라 +1로 넘어간다.
   ※ 노드 추가 시 **가이드 1-4b(다섯 곳 배선) + 4집합 일치 검사** 필수.
2. **주영님께 전달(강의) 진행 중** — 모듈3 노드2를 강의톤으로 전달 중이며 **1·2·3강까지 보냈다. 다음은 4강(출처와 우선권)**.
3. 적응기·모듈1·2는 기록이 끝났으므로 새로 열리는 노드만 따라가면 된다.

## 3. ★개인 계정에서 확인할 것 (주영님 요청)

**`FIXLOG-강의노트.md`의 [FIX-2026-08-25-1]** — 새로 쓴 3개 노드(27강)가 화면에 안 뜨던 결함을 찾아 고친 기록이다.
확인 방법: 강의노트를 브라우저로 열고 목차에서 **모듈2 노드8·노드9, 모듈3 노드1·노드2**를 눌러 본다.
내용이 뜨면 정상. **모듈1 노드1로 튕기면 재발**이다(그때는 JS `names`/`order`를 본다).
> ⚠️ 확인할 때 **브라우저 캐시**를 조심한다. 주소 뒤에 `?v=2`를 붙여 강제로 새로 받는다. 캐시 때문에 한 번 오판한 적 있다.

## 4. 최근 바뀐 규칙 (반영 완료, 어기지 말 것)

- **`git add -A` 금지** — `git add 00_log/`처럼 경로를 찍는다. (구 규칙 "동시 git 작업 금지"는 폐지) 커밋 직전 `git status --short`, push 전 `git pull`.
- **브리지는 append-only** — 새 글은 **맨 아래**에 붙인다. 최신이 아래다.
- **브리지 ID** = `KBTM-T-YYYYMMDD-N` (세션코드 **T** 필수) + `발신: 튜터` 줄.
- **노드를 새로 넣을 때 다섯 곳** 을 모두 고친다 → `강의기록-가이드.md §1-4b`. 빠뜨리면 조용히 깨진다.
- 커밋 스코프 분리(pre-commit 훅): 강의노트는 `content`(또는 `docs`) 스코프, 해커톤은 `hackathon`.

## 5. 미해결 · 주의

- **과정 종료일 불일치** — LMS 대시보드 `2027-01-15` vs 노션 1기 페이지 `2027-01-08` (7일 차). 퍼실 확인 전까지 **확정값으로 쓰지 않는다.**
- **폴더 재편 예정** — `Desktop\KDT\` 아래로 모으는 중이다. 브리지는 이미 심볼릭 링크(`KDT/00_shared/bridge`)로 바뀌었고, 튜터 집은 `02_tutor`가 될 예정. 매니저가 실행 전에 알린다.
- **노드7 과제 폴더명** — `03_module2/d_hw_N6-room-3d`가 실제로는 노드7 과제다. 제출된 채점 URL 보호를 위해 N6로 **원복**해 둔 상태이며, **채점 종료 후 매니저가 N7로 개명**할 예정이다. 그때까지 건드리지 않는다.
- 다른 축(해커톤 `90_hackathon/`)은 별도 세션 작업이다. 최근 커밋 대부분이 그쪽이니 log를 볼 때 헷갈리지 말 것.

## 6. 세션 시작 점검 3줄

```
git status --short
git worktree list
git branch -a
```
남의 미커밋, 숨은 worktree(`_worktrees/codex-kdt-controls`가 하나 있다), 모르는 브랜치를 5초에 확인한다.
