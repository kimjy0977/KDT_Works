# 튜터 세션 인수인계

> **다음 세션은 이 파일부터 읽는다.** 학습(강의노트·과제) 작업의 현재 상태와 다음 할 일을 담는다.
> 상태는 자주 바뀌므로 작업이 끝날 때마다 갱신한다. 규칙 정본은 `강의기록-가이드.md`, 사고 기록은 `FIXLOG-강의노트.md`.
> 최종 갱신 **2026-08-28** · 저장소 HEAD `21f0792` (동기화·미커밋 0)

---

## 1. 지금 어디까지 왔나

### 1-1. 모두연 LMS (`강의노트-모두연.html`)

| 구간 | 노드 | 강 | 상태 |
|---|---|---|---|
| 적응기 | adn1~5 | 24 | ✅ |
| 모듈1 | m1n1~7 | 53 | ✅ |
| 모듈2 | m2n1~9 | 75 | ✅ |
| 모듈3 | m3n1~4 | 39 | ✅ 열린 만큼 |
| **합계** | **25노드** | **섹션 191개 = 실제 강의 189강 + 프로젝트 제출 2** | 허브 8주차 |

> **숫자 두 개가 도는 이유(혼선 방지).** 노트의 강 섹션은 **191개**인데 허브 표기는 **189강**이다. 차이는 [프로젝트] 노드 둘 — **adn5**와 **m3n4** — 이 마지막에 `＋ 프로젝트 제출` 섹션을 하나씩 갖고 있기 때문이다. 같은 [프로젝트] 노드라도 **m2n5·m2n7은 제출을 섹션으로 만들지 않았다**(가이드 §1-2의 "(9강 + 프로젝트 제출)" 방식). **허브의 189가 실제 강의 수로 맞다.** 표기 방식이 노드마다 갈린 상태이니, 통일하려면 §5를 볼 것.

### 1-2. O'Reilly 도서 (`강의노트-오라일리.html`) — **전권 완료**

『에이전트 시대의 AI 시스템 설계』(원서 *Generative AI Design Patterns*, Lakshmanan·Hapke)

- **1~10장 · 32개 패턴 전원 기록 완료.** 감사에서 누락 0 확인.
- 모든 장이 **세 층**을 갖춤 — ①핵심 정리 ②💡강의 포인트 ③🎤강의톤 원고(접기 15개).
- 보강 2종: **패턴 상호참조 지도**(1장 개념→패턴, 패턴끼리의 연결, 증상→패턴 역방향 찾기), **실무 제약·한계 모음**.
- **남은 일은 전달(강의)뿐이다.** 접기를 펼치면 원고가 그대로 있으므로 새로 만들 필요 없다.

### 1-4. ★모듈3 노드5 메인 퀘스트 — **앱 완료·배포함** (2026-08-28)

`04_module3/a_mq_N5-myth-rag-guide/` · 배포 <https://kimjy0977.github.io/KDT_Works/04_module3/a_mq_N5-myth-rag-guide/>

MYTH GALLERY 공개 자료로 만든 **근거 인용 RAG 챗봇**. 노드4 설계 과제의 후속이고, 경계·이관·인젝션 대응은 그대로 승계했다.

| 항목 | 상태 |
|---|---|
| 자료 | 그리스로마 12점 → **41청크**. 모든 청크에 `url`, 전부 200 확인 |
| 근거 판정 | 문턱 0.20 미만이면 **생성 단계로 안 넘김**. 판정 5갈래(OK·근거없음·범위밖·모호·이관) |
| 수용 기준 | **7/7 통과** (`node tests/acceptance.js`) |
| 배포 | GitHub Pages 200 확인, 실기기 동작 확인 |
| 매니저 인계 | `[KBTM-T-20260828-3]` — 트래커·갤러리 카드 요청함 |

**⚠️ 아직 안 한 것 — 루브릭 원문 대조.** PRD·수용 기준은 개인 계정에서 읽은 메인 퀘스트 요건
(*공개 URL 자료 · 청크마다 url · 열어 보지 못한 URL 금지*)만 보고 썼다. **LMS 안내문 원문을 받으면
`PRD.md` 8절과 한 번 맞춰 볼 것.** 그리고 **노드5 강의노트는 아직 기록 전이다.**

---

### 1-3. 그 외 노트

`강의노트-앤트로픽.html`(앤트로픽 AI Fluency + 인프런) — 기존 유지.

---

## 2. 바로 다음에 할 일

1. **주영님께 전달(강의)**
   · 모두연 **모듈3 노드2**는 3강까지 전달 → **다음은 4강(출처와 우선권)**.
   · O'Reilly는 **1장만 전달**했다 → 2장부터는 노트의 접기 원고를 그대로 꺼내 쓰면 된다.
2. **모듈3 노드5 강의노트 기록** — 노드5는 이미 열렸고 과제는 끝냈다(1-4). **노트만 아직**이다. LMS 코스 `2171`. 노드3=`5159/26025~`, 노드4=`5160/26034~`. step URL은 순차라 +1.
   ※ 노드 추가 시 **가이드 §1-4b(다섯 곳 배선) + 4집합 일치 검사** 필수.
3. 적응기·모듈1·2는 기록이 끝났으므로 새로 열리는 노드만 따라가면 된다.

### 2-1. 이 세션(2026-08-28)에서 한 일

- 개인 계정 작업분을 **pull**로 이어받음(`d15a2bf` → 최종 `21f0792`).
- **`강의노트-오라일리.html` 신설 → 1~10장 전권 기록.** 처음엔 앤트로픽 노트에 Ⓒ 칸으로 넣었다가 **주영님 지시로 되돌리고 독립 파일로 분리**했다(기존 학습노트와 안 섞이게).
- **보강 2종 신설** — 패턴 상호참조 지도, 실무 제약·한계 모음.
- **2~10장 강의톤 원고 채움** — 이제 노트만 열면 그대로 강의할 수 있다.
- **`강의기록-가이드.md` 3부 신설** — 외부 도서 기준을 **수치로 고정**(전달 묶음 원문 1만~1.8만자 / 전달 1,500~1,900자 / 절 번호는 원서 그대로 / 세 층 / 음성 원고엔 표·기호 금지 / 한 줄 보고).
- **허브 `index.html`에 O'Reilly 카드 추가**(04번) + 카드 번호 05~08 재정렬 + 앤트로픽 노트 pagenav에 O'Reilly 링크. ⚠️ **허브는 매니저 관할이나 주영님 지시로 튜터가 직접 반영**했고 브리지 `[KBTM-T-20260828-2]`로 사후 통지했다.

- **모듈3 노드5 메인 퀘스트 제작·배포**(1-4 참조). PRD → 수집기 → 검색/판정 → 앱 → 수용 기준 7문항 → Pages 배포까지 한 바퀴.
  · 수용 기준을 코드로 적어 돌리다 **실제 버그를 하나 잡았다** — `"얼마야"` 안의 `"마야"`가 취급 밖 신화로 걸려 범위 밖 판정이 났다. 낱말 앞에서만 맞추도록 수정.
  · 커밋 `fbbb19a`(PRD) · `ad8dde1`(수집) · `2eb27eb`(앱).

### 2-2. 수확 방법 메모 (O'Reilly)

원문 페이지는 **비밀번호로 잠겨 있어 `curl`로 못 받는다**(잠금 페이지만 온다). 방법은 이렇다.
**브라우저(claude-in-chrome) 세션에서 `fetch`** → `localStorage`에 저장 → 헤더(`문제Problem`·`해법Solution`) 위치를 찾아 **구간별로 읽기**. 브라우저 반환은 2천 자쯤에서 잘리므로 한 번에 다 받으려 하지 말 것.

---

## 3. ★개인 계정에서 확인할 것

**(1) `FIXLOG-강의노트.md`의 [FIX-2026-08-25-1]** — 새로 쓴 3개 노드(27강)가 화면에 안 뜨던 결함 기록.
확인법: 강의노트를 열고 목차에서 **모듈2 노드8·노드9, 모듈3 노드1·노드2**를 눌러 본다. 내용이 뜨면 정상, **모듈1 노드1로 튕기면 재발**(그때는 JS `names`/`order`를 본다).

**(2) 허브에서 O'Reilly 노트로 들어가지는지** — `index.html`의 **04번 카드**.

> ⚠️ 둘 다 확인할 때 **브라우저 캐시**를 조심한다. 주소 뒤에 `?v=2`를 붙여 강제로 새로 받는다. 캐시 때문에 한 번 오판한 적 있다.

---

## 4. 규칙 (반영 완료, 어기지 말 것)

- **`git add -A` 금지** — `git add 00_log/`처럼 경로를 찍는다. (구 규칙 "동시 git 작업 금지"는 폐지) 커밋 직전 `git status --short`, push 전 `git pull`.
- **브리지는 append-only** — 새 글은 **맨 아래**. 최신이 아래다.
- **브리지 ID** = `KBTM-T-YYYYMMDD-N` (세션코드 **T** 필수) + `발신: 튜터` 줄.
- **노드를 새로 넣을 때 다섯 곳**을 모두 고친다 → 가이드 §1-4b. 빠뜨리면 조용히 깨진다.
- **외부 도서는 가이드 3부** — 개념 재서술만, 원문 문장·코드 예제 미수록, 열람 페이지 주소를 공개 노트에 남기지 않는다.
- 커밋 스코프 분리(pre-commit 훅): 강의노트는 `content`/`docs`, 해커톤은 `hackathon`.

---

## 5. 미해결 · 주의

- **[프로젝트] 노드의 제출 스텝 표기가 갈린다** — adn5·m3n4는 `＋ 프로젝트 제출`을 **강 섹션으로** 넣었고, m2n5·m2n7은 넣지 않았다. 통일한다면 **가이드 §1-2 방식**(머리글에 `(N강 + 프로젝트 제출)`로 적고 제출은 섹션으로 만들지 않음)에 맞추는 쪽이 규약에 부합한다. ※제출 섹션에 내용이 있을 수 있으니 **함부로 지우지 말고 먼저 열어볼 것.**
- **모두연 노트에서 다른 노트로 가는 링크가 없다** — 이 노트만 `pagenav`가 없다(과거에 "중복 pagenav 제거"로 의도적으로 뺀 것으로 보인다). 허브를 거치면 되므로 치명적이지 않다. **되살리려면 매니저와 조율**할 것.
- **패턴 19 이름이 갈린다** — 저자 공식 저장소 `Harness Engineering` vs 우리가 읽은 정리본 `Dependency Injection`. 노트에 **병기**해 뒀다. 검색할 때 둘 다 시도.
- **과정 종료일 불일치** — LMS 대시보드 `2027-01-15` vs 노션 1기 페이지 `2027-01-08`(7일 차). 퍼실 확인 전까지 **확정값으로 쓰지 않는다.**
- **폴더 재편 진행 중** — `Desktop\KDT\` 아래로 모으는 중. 브리지는 이미 심볼릭 링크(`KDT/00_shared/bridge`), 튜터 집은 `02_tutor` 예정. 매니저가 실행 전에 알린다.
- **노드7 과제 폴더명** — `03_module2/d_hw_N6-room-3d`가 실제로는 노드7 과제다. 제출된 채점 URL 보호를 위해 N6로 **원복**해 둔 상태이며 **채점 종료 후 매니저가 N7로 개명**한다. 그때까지 건드리지 않는다.
- 해커톤(`90_hackathon/`)은 **별도 세션** 작업이다. 최근 커밋에 많이 섞이니 log를 볼 때 헷갈리지 말 것.

---

## 6. 세션 시작 점검 3줄

```
git status --short
git worktree list
git branch -a
```
남의 미커밋, 숨은 worktree(`_worktrees/codex-kdt-controls`가 하나 있다), 모르는 브랜치를 5초에 확인한다.
