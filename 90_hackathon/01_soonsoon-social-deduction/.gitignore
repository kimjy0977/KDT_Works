# 회사 제공 원본·개인 사본 — 이 레포는 PUBLIC 이므로 절대 커밋 금지
_local/

# 비밀
.env
.env.local

# ⛔ SPUM 엔진·에셋 — 회사 라이선스 자산이라 이 공개 레포에 올라가면 안 된다.
#   원래는 proto/.gitignore 가 막고 있었는데, 2026-08-27 게임을 비공개
#   저장소로 옮기면서 **그 파일이 proto/ 와 함께 사라졌다.**
#   막는 규칙이 지켜야 할 대상과 같은 폴더에 있으면, 대상이 없어질 때
#   규칙도 같이 없어진다. 그래서 여기(상위)에 다시 적는다.
vendor/
proto/
