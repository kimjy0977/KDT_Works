# 인계 문서

## 0-최신. 해커톤 세션 9차 (2026-08-21 발표 당일 ~ 08-22) — **발표 완료** · 발표 후 정리

> **발표는 끝났다.** 채점도 끝나 제출 URL 위험이 사라졌다.

### 9-1. 발표 직전 (08-21) — 무대에서 보일 것들

| 커밋 | 내용 |
|---|---|
| `2e11158` | **태도가 서버에서 통째로 누락**돼 있던 것 — 대사 수렴의 진짜 원인 (work-rules **N-1**) |
| `9440566` | 롤식 대형 안내(`#shout`) + 투표를 한 명씩 흩뿌리기 |
| `7632469` | 대형 안내를 겹쳐 부르면 **진행이 영영 멈추던** 버그 |
| `e6b2ece` | 방에 들어오면 발밑에 「Space 잠자기」 안내 |
| `69e6510` | 되풀이 대사 필터(`similar.js`) + **동률이면 처형 없음** |
| `a94b498` | 자러 가는 시차 단축 — 마지막 사람이 7초 뒤에야 일어섰다 |

### 9-2. 발표 후 (08-22)

| 커밋 | 내용 |
|---|---|
| `aa5eeb3` | 그림 없는 캐릭터(곰곰이) **선택 차단** — 파일을 정본 삼아 자동 판별 |
| `5f1b327` | **평가 하네스** `tools/eval.mjs` + 공용 모듈 분리(`similar.js`·`mafia/attitudes.js`) |
| `63492cb` | 측정기가 게임과 다르게 부르던 것 수정 (**N-4**) |
| `84212bb` | **멀티플레이 설계안** `docs/multiplayer-design.md` |
| `724651c` | 팀 가이드 스크린샷에서 브라우저 크롬 제거 + **루트 `.gitignore`** |

### 9-3. 지금 상태

- **게임은 돌아간다.** 발표에서 시연했다.
- 로스터 10명 중 **9명 사용 가능.** 곰곰이만 그림이 없어 선택이 막혀 있다 —
  `assets/chars/gomgom-idle.{json,png}` 가 들어오면 **코드 수정 없이** 자동 해제된다.
- 딴청이는 `move` 시트가 없지만 **정상 동작**한다(걷는 동안 서 있는 자세로 보일 뿐).
- 이번 세션 SAM 사용: **평가 하네스 1회 36호출**이 전부. 게임은 한 판도 안 돌렸다.

### 9-4. ★ 다음 세션이 할 일

**① 폴더 개명 → `01_soonsoon-social-deduction`** ← **매니저 관할**

⛔ **9차 세션에서는 못 했다.** 그 폴더가 세션의 작업 디렉터리라 윈도우가 개명을 막는다
(work-rules **O-3**). **그 폴더를 안 쓰는 세션**에서 해야 한다.

고쳐야 할 참조 — 이미 다 찾아 뒀다:

| 위치 | 건수 | 비고 |
|---|---|---|
| 레포 루트 `index.html` | 1 | 갤러리 링크. **매니저 관할** |
| `90_hackathon/CLAUDE.md` | 3 | |
| `share/SPUM-팀가이드.md` | 5 | **GitHub 절대 URL** — 이미 배포됐으면 새 주소를 다시 알려야 한다 |
| `docs/` (handoff·rehearsal·team-onboarding×2·work-rules) | 5 | 상대경로 |
| `proto/tools/{grid-crop,mask-overlay,wall-mask}.mjs` | 3 | **절대 `file:///` 경로** — 상대경로로 바꾸면 윈도우 계정명 노출도 같이 해결된다 |
| `_local/` | 2 | 비커밋 |
| (레포 밖) 매니저 `KDT-공통운영규칙.md`·`과제-트래커.md` | 2 | 매니저 세션에서 |

**② 평가 재측정** — `node tools/eval.mjs --go --games 6`

도구는 완성·수정 완료. 첫 측정은 측정기 결함으로 무효였다(**N-4**).
유효했던 값: 지목 파싱 실패 0% · 정체 누설 0건 · 자기지목 0건 · 분석체 금지어 0%.
**미해결:** 캘리브레이션은 표본 6표라 판단 불가.
⚠ SAM 호출 ~40~50회 — **사용자 승인 후에** 돌린다.

**③ 「두 문장 초과 66.7%」** — 프롬프트가 "길어도 두 문장"을 요구하는데 3분의 2가 어긴다.
지시가 안 먹는다는 뜻이다. 비용 0으로 고칠 수 있고, 효과 확인은 ②와 묶는 게 효율적이다.

**④ 곰곰이 스프라이트** — SPUM Studio 로그인 필요. 사용자가 환경을 준비해 주기로 했다.

### 9-5. 판단이 바뀐 것

**과제정의서 원문을 직접 확인했다**(`_local/회사자료/…docx`, 전문 81문단).

> 「제약 사항」이 노출을 금지한 것은 **API 키뿐**이다. 대외비·기밀 조항은 문서 어디에도 없다.
> 오히려 과제 동기가 *"제품 쇼케이스/레퍼런스가 없어 외부 개발자 진입 장벽이 높다"* 이다.

→ `90_hackathon/CLAUDE.md §5` 의 **「파생 요약도 `_local/`」 규칙을 풀었다.**
   요건 요약·성공기준 정리는 커밋해도 된다. **원본 파일 재배포만** 막는다.
   규칙이 실제보다 빡빡하면 세션마다 같은 지적이 반복된다.

### 9-6. 미해결로 남기는 것

- **git 이력에 원본 스크린샷이 남아 있다.** 파일은 고쳤지만 이력은 그대로다.
  제거하려면 history 재작성 + force push — **비가역이라 사용자 판단 대기.**
- SPUM Studio 로그인 실패가 여러 세션째 이어진다.

---

## 1-이전. 해커톤 세션 8차 (2026-08-20) — 마피아 1판 완주 · Studio 한계 확정

### ✅ 끝났다 — 한 판 완주했고 **시민이 이겼다**

```
월드   「누가 내 케이크 먹었어? Copy」 WORLD_mt1bk999_KKZYXP · rev 249
시각   21:10 ~ 21:19 KST · 9분
비용   165 SSAM  (남은 잔액 25,400)
결과   최종 지목 오물오물 4 : 새침이 2 → 실제 마피아 적중, 시민 승리
전문   docs/logs/mafia-round-2026-08-20.md   ← 발표·데모 소재는 여기
상태   ⏸ Pause 완료 (sim off). 과금 안 됨
```

**`Play` 는 CDP 실클릭이면 먹는다.** 지난 세션의 `actors 0` 은 JS 로 만든
합성 클릭이 원인이었다. 브라우저 도구의 `left_click` 으로 누르면 `actors 6` 이 된다.
→ 이제 사람이 직접 누를 필요 없다.

### 이 판에서 새로 확정한 것 (§4-41~44 · feedback E-4·E-5)

| | 내용 |
|---|---|
| §4-41 | ⛔ **지시 프롬프트에 캐스트 명단이 없다** — 6/6 전원이 없는 이름을 지어냈다 |
| §4-42 | ⛔ **`role.goal`(비밀 역할)이 응답에 반영 안 됨** — 마피아가 2회 모두 부인 |
| §4-43 | 자율 짝대화는 **이동·조우가 트리거**로 보인다 (밀집 상태 9분간 0건) [추정] |
| §4-44 | 반대로 **진행자 지시는 6/6 무결·맥락 인용 5/5·형식 준수 6/6** |

> **한 문장 결론:** Studio 는 *진행자가 있는* 다인 대화극에는 충분하다.
> 없는 것은 **자율성**과 **비밀 정보 관리** 둘뿐이고,
> 그 둘이 정확히 마피아류를 막는다(E-3·E-4·E-5).

### 남은 일

| 우선순위 | 할 일 | 상태 |
|---|---|---|
| **1** | 발표 자료 카톡 전달 | 파일 준비 완료 · 사람이 보내야 함 |
| 2 | (선택) 스폰을 흩뿌리고 자율 대화 재현 — §4-43 반증 | 미실시 |
| 3 | (선택) 관계 「평가」 장시간 관찰 — §4-39 | 미실시 |

**하지 않기로 한 것** — Props/장소 인식 · 표 자동 집계 · `expert` 문제(SPUM 서버 쪽) ·
비밀 역할 게임을 Studio 안에서 억지로 성립시키기(§4-42 로 불가 확정)

### 발표 자료 (비커밋)

```
_local/발표자료/[김주영] SPUM_SAM_발표자료.md         ← 카톡 전달용
_local/발표자료/[신서연] SPUM_SAM_2분_발표자료_수정본.md  ← 참고용 사본
```

- 맨 앞에 **「이 문서로 발표자료를 만드는 AI 에게」** 지시 블록이 있다
- 개선 제안은 **A(재현 확인) / B(우리가 못 찾았을 수도)** 로 확신도를 갈랐다.
  **이 구분을 뭉개지 말 것**
- 회사 전달용 전체 피드백 = `docs/spum-feedback.md` (E-4·E-5 이번에 추가)

---


## 2-이전. 해커톤 세션 7차 (2026-08-20) — SPUM Studio 규명 + 발표 자료

> **팀 분업이 확정됐다.** 팀원 = HTTP/SPUM Engine 게임 고도화, 주영님 = **SPUM Studio 판**.
> 이 세션은 대부분 **Studio 를 규명하는 데** 썼고, 알아낸 것은 전부 문서에 적립했다.

### 지금 바로 할 수 있는 일 — **Play 버튼 한 번**

SPUM Studio 에 **마피아 판이 세팅을 마치고 대기 중**이다.

```
월드      「누가 내 케이크 먹었어? Copy」  WORLD_mt1bk999_KKZYXP
          ※ 원본 「누가 내 케이크 먹었어?」 는 건드리지 않았다
캐스트    6인 배치 완료
마피아    오물오물   (무작위 추첨 · 케이크 판 범인 살살이는 제외하고 뽑음)
목표      오물오물 "나는 마피아다…" / 나머지 5인 "나는 시민이다…"
월드목표  마피아 게임
모델      medium    (expert 는 호출이 통째로 실패 — §4-38)
저장      rev 230
```

⚠ **`Play` 는 사람이 직접 눌러야 한다.** 스크립트 클릭은 `sim on` 은 되지만
**`actors 0`** 이 되어 아무 일도 일어나지 않는다. 사용자 제스처가 필요한 것으로 보인다 ★★★☆☆.

**누른 뒤의 진행 순서** (지시 입력창에 순서대로)

```
[낮]   전체 → "마피아 게임을 시작합니다. 여섯 중 한 명이 마피아이며 각자 자기 역할만
              압니다. 지금은 낮입니다. 서로 이야기하며 누가 마피아인지 찾으십시오."
[밤]   대상 탭을 오물오물로 → "밤이 되었다. 오늘 밤 제거할 사람 한 명의 이름만 대라."
[아침] 전체 → "○○가 당했습니다"  + 그 캐릭터 runtimeEnabled OFF
[지목] 전체 → "한 명씩 차례로 이름 하나와 이유 한 문장"  → 발언을 사람이 집계
```

비용: 6인 자율 대화 **분당 약 250 SSAM**. 한 판 약 3~5분.

### 이 세션에서 확정한 것 (전부 `sam-connection-guide.md` 에 있음)

| 절 | 내용 |
|---|---|
| §4-27 | **대화 모드가 `baked` 였다** — 캐릭터가 안 살아있던 진짜 원인 |
| §4-29 | `llm` 모드 실측 — 짝 대화·교차 검증·의심 전가가 실제로 나온다 |
| §4-31 | Studio World 데이터 모델 전수 조사 (미확인 10건 해소) |
| §4-32 | Studio 만으로 마피아: 되는 것과 **구조적으로 없는 것** |
| §4-34 | 순순빌리지 영상 기능은 우리 Studio 에도 전부 있다 |
| §4-35 | **SAI 의 마을 요약은 미지원** (SAI 본인이 "준비 중"이라 답함) |
| §4-36 | `world.props` 키 자체가 없다 → 장소 이름 인식 불가 |
| §4-37 | **완전 자율 마피아는 실패한다** — 사회자가 4명이 된다 |
| §4-38 | **「소넷 4.6」 = `expert` 등급** (소스 확정). 단 우리 계정에서 expert 는 실패 |
| §4-39 | 「상호작용」 평가는 `setRelationshipMetrics` 로만 채워진다 |
| §4-40 | ⛔ **메모리 `reset` 이 캐스트 전체를 되돌린다** — 한 시간치 세팅이 날아갔다 |

### 발표 자료 (비커밋)

```
_local/발표자료/[김주영] SPUM_SAM_발표자료.md        ← 카톡 전달용. 344줄·한글 4,144자
_local/발표자료/[신서연] SPUM_SAM_2분_발표자료_수정본.md  ← 참고용 사본
```

- 맨 앞에 **「이 문서로 발표자료를 만드는 AI 에게」** 지시 블록이 있다.
  발표자님이 Claude 로 슬라이드를 만들 예정이라 그에 맞춰 넣었다
- 개선 제안은 **A(재현 확인) / B(우리가 못 찾았을 수도)** 로 확신도를 갈랐다.
  **이 구분을 뭉개지 말 것** — 섞이면 A 의 신뢰까지 깎인다
- 회사 전달용 전체 피드백은 `docs/spum-feedback.md` (8개 절·30여 건, 커밋됨)

### 남은 일

| 우선순위 | 할 일 |
|---|---|
| **1** | **마피아 한 판 완주** — Play 누르고 4단계 진행 → **JSONL 로그 확보**(데모 소재) |
| 2 | 발표 자료 카톡 전달 |
| 3 | (여유 시) 관계 평가가 실제로 붙는지 오래 돌려 확인 (§4-39) |

**하지 않기로 한 것** — Props/장소 인식(기능 없음) · 표 자동 집계(기능 없음) ·
`expert` 문제 해결(SPUM 서버 쪽) · 월드 3·4번째

### 자원

```
SSAM  약 29,900 / 45,000 남음     로컬 게임(Engine)은 완성 상태 · 손댈 것 없음
```

---

## 3-이전. 해커톤 세션 6차 (2026-08-20) — 게임 완성도 + 캐릭터 + 월드 착수

> **이 절이 최신이다.** 아래 절들은 이력이다. 커밋 32건.

### 1. ✅ 게임 — 지금 플레이 가능하다

```
시작 화면 → 모드(참여/관전) → 캐릭터 고르기 → 역할 공개
  ↓ 저녁 (최소 1명과 대화해야 진행)
  ↓ 밤   (범인이면 부엌 F 로 케이크 섭취 · 근처에 사람 있으면 목격당함)
  ↓ 아침 → 토론 (전체 채팅)
  ↓ 투표 → 승패 4분기 판정
```

| | 상태 |
|---|---|
| 거주자 | **6명** (두리번이 추가 — 의심 많은·잠귀 밝은 관찰자) |
| 캐릭터 시트 | **12/12** (6명 × idle+move) · 전부 SPUM 제작본 · **걷기 모션 있음** |
| 맵 | 32×32 · 통행 753칸 · 스폰 8곳 · `tileset_floorplan_v7b.png` |
| 범인 | **매 판 랜덤** (플레이어 포함) — `night.mjs` → `/api/newgame` |
| 모듈 | `src/` 11개 (setup·world·talk·npctalk·groupchat·phase·sprite·snap·ai·cast·main) |

**오늘 추가된 것:** 시작화면 · 역할공개 · 관전모드 · 캐릭터선택 · 전체맵(M) ·
새판/시작화면 버튼 · NPC끼리 대화 · **엿듣기 거리 170px** · 단계 통과조건 ·
범인 전용 밤 행동 · **전체 토론 채팅** · 승패 4분기

### 2. 🔄 진행 중 — 우주선 월드 (여기서 멈췄다)

```
우주선 격리구역   SMO_mt0w3m9s_GDXJ · 격자 64×64 · refs 4/10 · 타일 0   ← Slice 전
Custom SMO 4     32×32 · 973타일 ← 셰어하우스, 게임에 적용돼 있음
Custom SMO 5     16×16 · 0타일   ← ⚠ 빈 채로 생성된 것. 삭제 여부 사용자 확인 필요
```

**refs 4장을 뽑았고 마지막 것은 `sam-connection-guide.md` §4-25 정본 템플릿으로 생성했다**
(문 금지 + FLOOR CONTINUITY + 벽 제약). **아직 눈으로 확인하지 않았다.**

⚠ 이 시점에 **SPUM 로그인이 만료**돼 있었다.

### 3. 다음 세션이 이어받을 것 — 권장 순서

**① refs 4장을 먼저 눈으로 검토** (마스크 튜닝이 비싸므로 쓸 그림을 먼저 확정)
   - 문짝·해치가 없는가 / 바닥이 방까지 이어지는가 / 떠 있는 벽 토막이 없는가
   - 방이 16개 이상으로 조밀한가 (§4-24 — 격자만 올리면 안 넓어진다)

**② 채택본 Slice → 통행 마스크 튜닝** ← **병목**
   - 셰어하우스 값(색조 18~32 · 분산 44~70 · 밝기 ≤115)은 **금속 테마에 안 맞는다.**
     처음부터 다시 잡아야 한다. 절차는 `work-rules.md` K-3.
   - 도구: `tools/wall-mask.mjs`(판정+도달검사) · `tools/mask-overlay.mjs`(눈으로 확인)
     · `tools/grid-crop.mjs`(좌표 라벨)

**③ 우주선 `map.json` 생성** — `tools/build-map.mjs` 의 SPAWNS 를 우주선 방 위치로 교체
   (지금은 셰어하우스 좌표가 박혀 있다). 항등 매핑 규칙은 K-1.

**④ 월드 선택 UI + 나머지 3개 월드** (산장·호텔 등)

### 4. 알아 둘 것

- **가로로 긴 맵(60×40)은 그대로 안 된다.** 출력이 1024 고정이라 격자는 16/32/64/128 만
  가능하다(§4-23). 64×64 로 뽑아 **로컬에서 중앙 40행만 잘라야** 한다.
- **격자를 키운다고 맵이 넓어지지 않는다**(§4-24). 프롬프트 밀도를 올려야 한다.
- 새 SMO 는 **격자 16×16 · 품질 Low** 로 시작한다(L-7). 생성 전 둘 다 확인.
- 시트 생성은 **크롬 창이 화면 앞에 있어야** 한다(L-1).

### 5. 이번 세션에 기록한 지식

| 파일 | 절 |
|---|---|
| `sam-connection-guide.md` | §4-19~4-25 (World 시뮬 대사 · decisionMode · 스프라이트 · 문 금지 · 격자 제약 · **격자≠넓이** · **프롬프트 정본 템플릿**) |
| `work-rules.md` | J절(refs·저장복구) · K절(맵 항등매핑) · **L절 8건**(숨은탭·Export함정·1px테두리·React입력 등) |
| `team-onboarding.md` | **신규.** 팀원 인수인계 + AI 에이전트 규칙(비용·비가역·키) |
| `tutorial.md` | §6 전면 정정 · §7-b 신규 |

---

## 0-이전. 해커톤 세션 6차 전반 — 맵 적용 + NPC 끼리의 대화

> **이 절이 최신이다.** 아래 절들은 이력이다.

### 1. ✅ 평면도 v7b 를 SPUM 맵·World 에 적용 (rev 128)

프롬프트 3회 생성 → v7b 채택 → Slice(1024칸/973리소스) → 맵 44x32 → **32x32**.
back_1 항등 매핑 1024칸 · 통행 753 · 막힘 271 · **방 9곳 전부 도달**.
침대는 통행 가능(NPC 가 올라가야 한다). 보라 침실 문간 4칸을 손으로 열었다.

⚠ **게임(`proto/assets/map.json`)은 아직 옛 시트다.** Studio 만 v7b 다.
맞추려면 `tileset_floorplan_v7b.png` 로 `floorplan-mask` → `build-map` 재실행.

세 번 실패한 원인과 정답은 `work-rules.md` **K절**. 요약: SPUM 은 테마를 맵에 넣을 때
격자 1칸당 타일 1개(1024개)를 만든다. `back_1[y*32+x] = tileIdBase + y*32 + x` 가 전부다.

### 2. ✅ NPC 끼리의 대화 — 구현·검증 완료

SPUM `ConversationModel`(제안→세션→턴→종료)을 우리가 구동한다. 각자 **자기 기억만** 본다.

- `engine.mjs` — `rawGenerate`(cleanLine 분리) · `npcTurnPrompt` · `parseTurn` · `askNpcTurn`
- `server.mjs` — `POST /api/npc-turn`. **속마음은 응답에서 뺀다**(정답에 가깝다). 감정은 내보낸다
- `src/npctalk.js` — 근접 80px 짝짓기 · 최대 6왕복 · 쿨다운 25초 · 말풍선
- `tools/npc-talk-check.mjs` — 브라우저 없이 검증

**게임 안 실측(폴짝이↔살살이 6턴):** 범인이 4번째 턴에 "화장실 다녀왔다"고 해놓고
6번째 턴에 소매 얼룩을 "부엌에서 뭔가 만지다가"라고 말해 **스스로 모순을 만들었다.**
플레이어가 잡아낼 수 있는 단서가 저절로 생긴다. 말풍선·HUD 표시도 눈으로 확인했다.

> **World 시뮬레이터를 안 쓴 이유:** 대사는 잘 만들지만 모든 턴이 `creator_response` 라
> 다섯이 각자 창조주에게 답할 뿐 **서로 주고받지 않는다**(`sam-connection-guide.md` §4-19).

### 3. 다음 세션이 이어받을 것

1. **게임 맵을 v7b 로 교체** — `floorplan-mask` → `build-map` 재실행(로컬 작업)
2. **Cast Editor 에서 5명 시트 추출** — idle 2/5 · walk **0/5**. 인물 구분이 안 된다(A-4 함정)
3. **`night.mjs` 서버 연결**(`/api/newgame`) — 500판 검증 끝, 선만 안 꽂힘. 지금은 범인 고정
4. 가구 통행 차단 보강 (벽은 완료)

---

## 0-현재. 해커톤 세션 5차 (2026-08-19 밤) — 저장소 해결 · 평면도 회수

> **이 절이 최신이다.** 아래 §0-Z(4차)·§0-A(3차)는 이력이며, 그 안의 "적용 완료" 서술은
> 지금과 다를 수 있다. **작업 전에 해시를 직접 읽을 것**(work-rules J-2).

### 1. ✅ 서버 저장 복구 — 세 세션 만에 해결

`studio_state_object_missing` 교착을 규명하고 실제로 풀었다. **rev 111 생성.**

- 원인: 서버가 아니라 **클라이언트 교착**. 읽기(500)가 실패하면 `_serverSync.enabled` 가
  false 로 남고, 저장이 첫 줄에서 차단돼 **HTTP 요청조차 안 나간다.** 401/404 는
  정상 경로를 타는데 **500 만 예외로 빠진다.**
- 해결: `GET /api/studio/state` 응답만 빈 상태로 만들어 주면 클라이언트가 스스로 로컬을 올린다.
- 결과: state/storage 200 · 활성 rev 111 · 용량 배지 "확인 실패" → **911 MB left** ·
  패치 없이 `saveServerSnapshot('manual')` → `true`

→ **수동 백업 강박 끝.** 근거·재현 = `spum-feedback.md` A-6 / 재발 시 복구 = `work-rules.md` J-4

### 2. ✅ 잃어버린 평면도 회수

주영님이 아끼던 32×32 평면도를 **114MB 백업(15:04)** 에서 찾았다. 스토리지 정리 때 지워진 것.

- `proto/assets/tileset_floorplan_v6.png` — 원본 (해시 `6609f003f90f`)
- `proto/assets/tileset_floorplan_v6open.png` — **중앙 거실 벽 4면 제거본**

거실이 **문 하나 없는 밀폐 상자**였다(NPC 가 영영 못 들어감). 벽이 32px 격자에 안 맞아
칸이 아니라 픽셀 사각형으로 다뤄 마루 무늬 위상을 유지한 채 덮었다(39,312px).
⚠ 위쪽은 한 겹만 열렸다 — 그 위 row 8~9 벽은 그대로다. 주영님 확인 대기.

### 3. ✅ localStorage 96% → 52.6%

`셰어하우스 인테리어`(옛 173타일, 지금 미사용)의 localStorage 사본 4.36MB 삭제.
원본은 IndexedDB 에 그대로라 안전했다(실측 확인). 한도는 **5,239,745자 ≈ 10MB(바이트)**.
"5MB"는 문자 수 기준이라 문서와 모순 없음. 근본 원인은 IndexedDB/localStorage **중복 저장**
(`spum-feedback.md` A-4).

### 4. ⚠ 정정 — Studio 테마는 v5 가 아니다

§0-Z.3-1 의 "v5 가 Studio 에 적용돼 있다"는 **지금 사실이 아니다.** 다섯 테마 전부 확인:

```
Custom SMO 4      32x32  696타일  bdbc02f3 = v2   ← 게임 맵이 쓰는 테마
Custom SMO 3      16x16   54타일  fb4cad5a
셰어하우스 인테리어  16x16  173타일  acee7ca9
Custom SMO 1      16x16  185타일  35ae4e6a
기본 맵 데이터      16x16   20타일  34b5f7ff
```

**어느 테마도 v5 를 기준으로 쓰지 않는다.** 재부팅 후 백업 복원이 되돌린 것으로 보인다[추정].
또한 이 세션에서 refs 썸네일을 클릭했더니 빈 값이 v2 로 바뀌었다 —
**refs 클릭은 읽기 전용이 아니다**(work-rules J-1).

맵 배선 자체는 정상: `MAP_msysua8z_CKYO` 44×32 · 테마 `SMO_mszq4o9l_DWAN` base 4097 ·
back_1 이 4097~5120 항등 매핑(1024칸) · walkable 739 · obstacle 669.

### 5. 다음 세션이 이어받을 것

1. **v6open 을 Custom SMO 4 의 슬라이스 기준으로 넣기** ← 본작업 재개 지점
   - 미해결: 로컬 이미지를 Studio 테마 소스로 넣는 경로. `+ Add Source` 는 img2img 입력 전용.
   - 후보 ①: `+ Add Source` 로 v6open 넣고 최소 프롬프트 img2img → 결과가 refs 에 남으면 Slice
   - 후보 ②: IndexedDB `themeStates` 에 직접 쓰기 (확실하지만 앱이 덮어쓸 위험)
2. **캐릭터 간 대화** — 주영님이 지목한 다음 기능
3. Cast Editor 에서 5명 시트 추출 (idle 2/5 · walk 0/5 · A-4 함정 주의)
4. `night.mjs` 서버 연결 (`/api/newgame`) — 500판 검증 끝, 선만 안 꽂힘

---

## 0-Z. 해커톤 세션 4차 (2026-08-19 저녁) — 상태 점검 · 백업 · 저장소 원인 규명

> 짧은 세션이었다. **코드보다 사실관계 정정이 수확이다.** 아래 3-1~3-3 은 이 문서의
> 옛 서술을 뒤집는다. 다음 세션은 이 절을 먼저 읽고 §0-A 의 "다음 세션이 이어받을 것"을
> **그대로 믿지 말 것.**

### 1. 🔧 서버 저장소 실패 — 원인 규명 (★★★★★ API 직접 호출)

**"서버 저장이 막혀 있다"는 서술은 부정확했다. 막힌 건 저장이 아니라 *상태 객체*다.**

| 엔드포인트 | 결과 |
|---|---|
| `/api/studio/revisions` | ✅ **200** — `activeRevision: 110`, 리비전 목록 정상 |
| `/api/studio/state` | ❌ **500** `studio_state_object_missing` |
| `/api/studio/storage` | ❌ **500** `studio_state_object_missing` (상태 객체를 읽어 용량을 계산한다) |
| `/api/me` | ✅ 200 · Creator Pro · SSAM 43,130 left |

**진단:** 서버 DB 에 리비전 110 은 **등록돼 있는데, 그 리비전이 가리키는 상태 blob 이
스토리지에 없다** — 끊어진 포인터다. 그래서 ① 용량 조회 실패 ② 상태 로드 실패
(하단 배지 "서버 Studio 저장소 연결 실패") ③ 그런데 **스냅샷 목록(rev 107~110)은 보인다**
는 모순된 증상이 동시에 나온다.

rev 110 생성 시각 `2026-08-19T05:47:21Z` = **14:47 KST**. 즉 그때부터 끊겨 있었다.
ACCOUNT 패널 기준 rev 110 은 오브젝트 **4**, rev 107~109 는 오브젝트 **5** (현재 로컬은 5).

> **다음 세션이 시도할 것:** ACCOUNT → **「수동 스냅샷 저장」** 으로 rev 111 을 만들어
> 본다. 성공하면 새 상태 객체가 생겨 끊어진 포인터가 해소될 가능성이 있다. **[추정]**
> ⚠ 쓰기 동작이므로 **반드시 백업을 받은 뒤** 시도할 것.

### 2. ✅ 백업 받아 뒀다

`다운로드/spum-studio-backup-20260819080737.json` · **61.6MB** · 17:07.
검증(★★★★★): v5 슬라이스 해시 포함 · `sv_studio_*_v1` 5키 전부 · `MAP_msysua8z_CKYO` 포함.

### 3. 문서 정정 — §0-A 의 "다음 세션이 이어받을 것" 재판정

**3-1. 「v5 를 Slice → Studio 맵에 적용」은 이미 끝나 있었다.**
Studio 테마 `SMO_mszq4o9l_DWAN` 의 `sliceBaseAssetId` 가
`sha256:536b1296936a574ad27b2b7b3fd2b8d23965494745bab09d9bb05dfbbee25859` 인데
이는 로컬 `assets/tileset_floorplan_v5.png` 의 sha256 **64자리 전부와 일치**한다.
World Editor 에서 평면도가 렌더되는 것도 **눈으로 확인**했다(침실 5 · 중앙 거실 · 캐릭터 5명).

- Studio 맵: 44×32 캔버스 · 테마 `tileIdBase 4097` · 32열 · back_1 이 4097~5120 **항등 매핑**
- 나머지 12열은 obstacle 처리 → obstacle 669 = 로컬 285 + 빈 영역 384. **로컬과 구조가 같다**(walkable 739 일치)
- ⚠ 다만 **게임이 쓰는 시트는 아직 `tileset_floorplan.png`(v2 계열)** 이다.
  Studio=v5 / 게임=v2 로 **그림이 갈라져 있다.** 맞추려면 `floorplan-mask.mjs` → `build-map.mjs` 재실행(로컬 작업, SPUM 불필요).

**3-2. 스프라이트 문제는 "외형이 없다"가 아니라 "추출이 안 됐다"이다.**
Studio World 에서 **5명이 전부 다른 외형으로 보인다**(주영님 확인). 즉 저작은 끝나 있고,
로컬에 고유본이 3장뿐인 것은 **Cast Export 함정(A-4)** 때문이다.
로컬 현황: 초상 `cut/` 5장 중 고유 3장(꾸벅이=새침이, 오물오물=살살이) · idle 시트는 새침이·폴짝이 2명분.
→ 할 일은 **Cast Editor 에서 5명분을 다시 뽑고 `characterName` 으로 건건이 검증**하는 것.

**3-3. 「기술문서 맵 파이프라인 절」은 전 세션이 이미 넣었다**(커밋 `2175dd8`).

### 4. 코드 — 미커밋 상태로 남겨 둔다

| 파일 | 상태 |
|---|---|
| `proto/src/snap.js` (신규) | 좌표를 걸을 수 있는 칸으로 끌어당기는 순수 함수 ✅ 완료 |
| `proto/tools/places-check.mjs` (신규) | 맵 교체 후 목적지 검사기 ✅ **12/12 통과** |
| `proto/src/ai.js` `world.js` `phase.js` (수정) | `snapPlaces()` 연결 + `phase.js` 의 중복 링 탐색 제거 |
| `proto/night.mjs` (신규) | 밤 시뮬레이터 — **서버 미연결.** 게임은 여전히 고정 시나리오 |

**고친 버그:** 맵이 40×30 → 32×32 로 바뀌면서 **배회 목적지 12곳 중 3곳(욕실·중앙 복도·새침이 방)이
벽에 묻혀 있었다.** 길찾기가 조용히 실패해 그 장소는 영영 방문되지 않는다.
좌표를 고치는 대신 부팅 때 스냅하게 했다(맵을 또 갈아엎어도 견딘다).

**`night.mjs` 검증(500판 결정론적):** 범인 분포 89~112(고름) · 목격자 없는 판 45%(의도대로 비보장) ·
레드헤링 71% · **기억 자기모순 0 · 풀 수 없는 판 0**. 연결만 하면 §2-1 랜덤 배정이 성립한다.

### 5. 🔥 새 함정 — bash heredoc 이 템플릿 리터럴을 먹는다

`node -e "... `${x}` ..."` 처럼 **bash 큰따옴표 안에 백틱**을 넣으면 셸이 명령 치환으로 해석해
**파일이 조용히 깨진다.** 실제로 `night.mjs` 가 한 번 파괴됐고, 에러는 엉뚱한 곳에서 났다.
→ 템플릿 리터럴이 든 코드는 **따옴표 heredoc(`<<'EOF'`)** 이나 **Write 도구**로 쓴다.

---

## 0-A. 해커톤 세션 3차 (2026-08-19) — P1 완료: 근접 대화 + AI 이동

**과제 성공기준 §2(NPC 3명 이상과 자연어 대화)·§3(대화 맥락 유지)를 실제로 충족시켰다.**

| 산출물 | 내용 |
|---|---|
| `proto/src/talk.js` | 근접 감지 → `E` → 대화창 → `/api/ask` → 답변 + 말풍선. 캐릭터별 history |
| `proto/src/ai.js` | `Wander` — NPC 4명이 집 안 11개 지점을 오간다. 이동은 `NavAgent` |
| `proto/src/world.js` | 기존 게임 셸을 모듈로 분리 + 길찾기 그리드 추가 |
| `proto/src/cast.js` `src/main.js` | 표시정보·배회 목적지·예시질문 / 부팅 |
| `proto/game.html` | 화면 껍데기만 남기고 로직을 `src/` 로 뺐다 |
| `proto/engine.mjs` `server.mjs` | `cleanLine()` — 모델이 섞는 `*지문*` 제거 |

**설계 — 맥락 유지를 어떻게 했나:** 서버는 무상태다. 클라이언트가 캐릭터별로
`[질문, 답변]` 쌍을 누적해 매 요청에 통째로 실어 보낸다(`talk.js` histories, 최근 12턴).
그래서 대명사만 쓴 후속 질문이 통하고, 압박 3연타 같은 누적 심문이 성립한다.

**검증 (★★★★★ 직접 확인 · 결정론적):**

| 항목 | 결과 |
|---|---|
| AI 이동 | 4명 전원 10초에 195~318px 이동 |
| 벽 침범 | **60초 시뮬 14,400표본 중 0건**, 196종 타일 방문 |
| 대화 (UI 경유) | 폴짝이·살살이·꾸벅이 **3명** 성공. 성격 반영 뚜렷(감탄사 / 능글 거짓말 / 말끝 흐림) |
| 맥락 유지 | "그 사람, 다음 날 아침에도 봤어?" → **"살살이 소매에 붉은 얼룩"** 으로 응답 |
| 대화 중 정지 | 대상 NPC 3초간 0px, 나머지 NPC 는 계속 이동 |
| 대화 종료 후 | 재개 확인. 재대화 시 이전 로그 복원 |
| 말풍선 렌더 | 흰 픽셀 317 → 33,861 (실제로 그려짐) |
| `cleanLine` | 단위 4/4 · 서버 응답에서 `*지문*` 사라진 것 확인 |

> ⛔ **육안 확인은 못 했다.** 브라우저 창이 표시되지 않아 스크린샷이 안 잡혔다(픽셀 카운트로 대체).
> **주영님이 `bash serve.sh` 로 한 번 눈으로 봐 주셔야 한다.**

**새로 판 함정 (→ `CLAUDE.md` §5-6~5-8 에 정리):**
- 엔진은 `update()` 예외 시 컴포넌트를 **조용히 끈다.** 콘솔에도 안 찍힌다.
- `BubbleRenderer` 는 루트 `index.js` 에 export 가 없다.
- `buildFromTileMap()` 은 우리 맵에 안 맞는다 → `obstacle` 레이어를 `setGrid()` 로 직접.
- Git Bash 에서 한글을 curl 로 POST 하면 인코딩이 깨져 `unknown character` 가 난다.
  **코드 문제로 오인하기 쉽다.** API 수동 테스트는 `node -e` 로 할 것.

### 이어서 — 평면도 v5 · Studio World 반영 (2026-08-19, 해커톤 세션 3차 종료)

**맵을 AI 평면도 한 장으로 바꿨다.** 조각 재조립을 버리고 Object Editor 격자를
32x32 로 올려 "집 전체 평면도"를 통째로 생성한다. 1024x1024 / 셀 32px 이라
게임 tileSize 와 1:1 이다.

**생성 5회의 교훈 (프롬프트 설계 — 이게 이번 세션 최대 수확):**

| 시도 | 프롬프트 성격 | 결과 |
|---|---|---|
| v1 | "seamless, edge-matching" (추상) | 넓게 깔 타일 0장 |
| v2 | "평평한 단색 면" + 방 목록 (구체) | **좋음** — 부엌·욕실·색색 침실 |
| v3 | "큰 거실 / 넓은 복도" 강조 | 중앙이 전부를 먹어 벽 자리가 사라짐 |
| v4 | 칸 수를 숫자로 지정 | 구조는 잡혔으나 부엌·욕실 소실 · 돌벽 |
| **v5** | **v2 프롬프트 + "중앙 거실엔 벽이 없다" 한 줄만 추가** | **✅ 전 조건 충족** |

> **되는 프롬프트에 조건 하나만 더한다.** 추상어는 과잉 반응하고, 수치는 구조만
> 잡고 내용을 흘린다. 전면 재작성보다 국소 추가가 안전하다.

**현재 상태**

| | |
|---|---|
| Studio World | ✅ 평면도 적용됨 · 맵 44x32 · 캐릭터 5명 배치 (눈으로 확인) |
| `tileset_floorplan.png` | v2 + 벽 6칸 제거본 (게임이 쓰는 것) |
| `tileset_floorplan_v5.png` | **최신 생성물 — 아직 미적용** |
| 통행 마스크 | `floorplan-mask.json` · 통행 745칸 |

**다음 세션이 이어받을 것**

1. **v5 를 Slice → Studio 맵에 적용** (절차 `sam-connection-guide.md` §4-10)
2. **적용 직후 「내 Studio 데이터 다운로드」로 백업** — 서버 저장이
   `studio_state_object_missing` 으로 막혀 있어 **브라우저에만 남는다.** 필수다
3. 게임 시트도 v5 로 교체 → `floorplan-mask.mjs` → `build-map.mjs` 재실행
4. 캐릭터 걷기 모션 — `SheetAnimator` 는 만들어 뒀고 **walk 클립 추출만** 남았다
   (Cast Editor 하단 `IDLE/MOVE/ATK/DMG/DEATH` 중 MOVE. A-4 함정 주의)
5. 기술 문서에 맵 파이프라인 절 추가

**⚠ 오늘 데이터를 두 번 잃었다.** 원인과 복구법은 `work-rules.md` I절 참조.
백업 JSON 은 `다운로드/spum-studio-backup-*.json` 에 4개 있다.

---

### 이어서 — AI 타일 생성 → 러그 적용 (2026-08-19)

주영님 아이디어1(Object Editor 로 조각 생성)을 **실제로 돌려 성공**했다.

**①(기존 시트 재배치)은 측정으로 불가능 판정.** `tools/tile-report.mjs` 전수 측정 결과
넓게 깔 수 있는 타일이 256장 중 6장뿐이고 **전부 갈색 원목**, 비-갈색은 **0장**이었다.

**②(새 조각 생성)는 2차 시도에서 성공.** 1차 대비 두 가지만 바꿨다 —
품질 `low`→`high`, 프롬프트를 추상어(`seamless`)에서 구체어(`평평한 단색 면`)로.

| | 기존 시트 | 1차 | 2차 |
|---|---|---|---|
| 안전(≤40) | 6 | 0 | **2** |
| 최고점 | 32.7 | 67.3 | **27.6** |
| 자동 분류 | — | obstacle_slowing 32 | **floor 53 · 전부 passable** |

**적용 결과 — 맨바닥 91% → 71%** (러그 66 → 215칸, 구역 점유율 22~30% → 56~66%).

- `proto/assets/tileset_rug.png` (192×192 · 셀 12px — 기존 시트와 동일 규격)
- `map.json` 에 `tilesets[]` 추가, 레이어마다 `tileset` 지정
- `world.js` 가 **시각 레이어마다 타일맵을 하나씩** 만든다(`audit §6-7`)

**검증 (★★★★★):** 타일맵 3장 생성 · 시트 2장 로드(main@2049 · rug@4097) ·
거실에서 남색 러그 화면 **31.6%** · 침실에서 초록 **12.4%** · 20초 시뮬 벽 침범 0건 · 경고 없음.

> **한계:** 프롬프트로 6색(남색·초록·빨강·크림·회색·나무)을 요청했는데
> **평평하게 나온 건 2색뿐**이다. 색을 늘리려면 재생성이 필요하다.
> `front_1` 덮개 조각은 아직 못 만들었다.

---

### 이어서 — 맵 레이어 스택 재구성 (2026-08-19)

주영님 아이디어2(레이어 스택)를 채택해 맵 데이터 구조를 갈랐다.
**SPUM 탐색에서 `기본맵`이 이미 `back_1·back_2·front_1` 을 쓰는 것을 확인**한 뒤 진행.

```
front_1 (+1)  덮개 — 캐릭터 앞          ← 아직 비어 있다(§ 한계)
────────  0   Character
back_3  (-1)  가구  131칸
back_2  (-2)  러그   66칸
back_1  (-3)  바닥·벽 1200칸
```

- `map.json` 을 **SPUM 과 같은 `layers[]` 배열**로 바꿨다(순서 = 렌더 순서, 변환 없이 밀어넣기 가능)
- `stamp()` 가 바닥을 건드리지 않는다 → **가구를 지우면 바닥이 저절로 드러난다**(비파괴)
- 레이어 판정은 의미 기반: **막힌 칸의 그림 = 가구 / 밟는 칸의 그림 = 러그**
- `world.js` 는 앞/뒤 타일맵을 **별도 GameObject** 로 만든다(엔진 제약 — `audit §6-5`)

**새로 생긴 검증 (빌드가 실패한다):**
- 바닥 구멍 0 · 러그가 막은 칸 0 · 벽에 걸쳐 강등된 칸 수 로그
- **밀도 지표: 맨바닥 91%** ← "휑하다"의 정체. 개선 목표 숫자가 생겼다

**검증 (★★★★★):** 스폰 7곳 연결 유지 · 타일맵 3장 렌더 확인 ·
20초 시뮬 벽 침범 0건 · frameWarnings 없음.

> **한계:** `front_1` 은 **우리 시트로는 채울 수 없다.** 가구가 불투명 완성 도면이고
> 가구 칸은 전부 obstacle 이라, front 로 올려도 가려질 캐릭터가 없다.
> 문틀 상인방·아치 같은 **새 조각이 필요하다 → Object Editor 작업**(아이디어1과 연결).

**다음:** ① 맨바닥 91% 낮추기(러그 확장) ② Object Editor 로 덮개·프롭 조각 생성
③ `map.objects[]` 로 프롭 배치 ④ 스프라이트 시트 재생 컴포넌트

---

### 이어서 — 게임 루프 완성 (`src/phase.js`)

`저녁 → 밤 → 아침 → 토론 → 투표 → 판정` 전 구간 동작.
**단계는 시간이 아니라 플레이어 행동으로 넘어간다** — 타이머로 넘기면 SAM 응답을
기다리는 몇 초 사이에 화면이 바뀌어 버린다.

- **저녁·아침·토론**: NPC 4명을 거실로 소집(`gather`)
- **밤**: 각자 방으로 귀가(`sendHome`) + **시야 제한** — 월드 좌표에 방사형 그라디언트를
  그린다. 화면 오버레이로 하면 카메라 클램프(맵 가장자리)와 어긋난다
- **밤 상호작용**: 부엌 근처에서 `F` → 냉장고 확인 / NPC 와 마주치면 목격 기록
- **투표**: 5명 + 각자 몇 번 심문했는지 + 밤에 마주친 사람 요약 → `/api/verdict`

**검증 (★★★★★ 직접 확인):** 전 단계 전이 · 저녁 4명 전원 거실 도착(0~90px) ·
밤 4명 전원 자기 방 도착(0px) · 시야 평균밝기 114→46(중앙 91 vs 가장자리 8) ·
냉장고 F · 목격 기록("폴짝이 방에서 폴짝이를 마주쳤다") ·
투표 오답/정답 **양쪽** 경로 · 토론 중 심문 정상.

**여기서 잡은 버그 2건:**
1. 거실 모임 지점 4곳 중 2곳이 **라운지 가구 위**(20,14 / 18,14)라 길찾기가 실패했고,
   `Wander` 가 명령을 조용히 버린 채 배회했다 → 걸을 수 있는 칸으로 **스냅**(`snap()`).
   **좌표를 손으로 찍으면 반드시 walkable 검사를 붙일 것.**
2. `"오물오물는 범인이 아니었다"` — 한국어 조사가 틀렸다 → `josa()` 로 받침 판정(5명 전원 확인).

> **아직 안 된 것:** `game-spec.md` §2-1 **범인 매 판 랜덤 배정**(플레이어 포함).
> 지금은 고정 시나리오 한 판(범인=살살이)이라 재플레이 가치가 없다.
> 서버에 밤 시뮬레이터 + `/api/newgame` 이 필요하다.

**다음 시작점: 기술 문서**(성공기준 §4 · 1급 산출물). 그 다음 밤 시뮬 랜덤화.

---

## 0. 해커톤 세션 2차 (2026-08-19) 갱신 — 맵을 다시 만들었다

**진단:** 월드가 휑했던 진짜 원인은 맵 데이터였다. 173타일 테마 중 **4개만** 쓰고 있었고,
가구 레이어 `front_1` 은 **1200칸 전부 0**(빈 값)이었다. 게다가
거실 바닥엔 **옷장 타일**(시트 1,1)이, 부엌 바닥엔 **화분 타일**(시트 4,1)이 깔려 있었다.
→ 타일이 없어서가 아니라 **배치를 안 해서** 휑했던 것.

**해결 기법 — "스탬프":** 테마 시트는 낱개 타일 모음이 아니라 **완성된 인테리어 도면**이다.
가구가 여러 셀에 걸쳐 그려져 있어(침대 1x2, 조리대 4x1, 라운지 5x4) 한 셀만 반복하면 무늬가 깨진다.
그래서 시트의 **직사각 영역을 통째로** 맵에 찍는다. 원본 도면의 완성도가 그대로 옮겨진다.

| 산출물 | 내용 |
|---|---|
| `proto/tools/build-map.mjs` | 맵 생성기. 스탬프 8종 + **자체 연결성 검증**(스폰 7곳이 서로 도달 가능한지, 실패 시 빌드 중단) |
| `proto/tools/png.mjs` | 의존성 없는 PNG 디코더/인코더 — 브라우저 없이 맵·타일을 눈으로 검증 |
| `proto/tools/atlas.html` | 타일 시트 격자 뷰어 |
| `proto/assets/map.json` | 재생성. **사용 타일 4종 → 81종**, 침실 5 · 부엌 · 욕실 · 라운지 가구 배치 |

**검증한 것 (★★★★★ 직접 확인):** 미리보기 렌더로 눈 확인 · 스폰 7곳 연결성 통과.
연결성 검사가 실제로 버그 3건을 잡았다 — 조리대가 부엌 문을, 소파가 폴짝이 방을 봉인, 거실 스폰이 탁자 위.

**같이 고친 것:** `server.mjs` 가 모든 정적 파일을 `text/plain` 으로 주고 있었다
→ **`.js` MIME 이 틀리면 브라우저가 ES 모듈 import 를 거부**하므로 SPUM Engine 붙이기 전에 필수였다. 확장자별 MIME 로 수정.
`index.html` 타일 좌표 off-by-one(1-based 를 0-based 로 읽음)도 수정.

**SPUM 반영 완료 (★★★★★ 눈으로 확인):** A-1 절차대로 `sv_studio_maps_v1` 의
`MAP_msysua8z_CKYO` 4개 레이어를 교체 → `saveServerSnapshot("manual")` → 새로고침.
**서버 rev 77** 로 저장됨. Map Editor·World Play 양쪽에서 가구가 보이는 것 확인.
SPUM 저장 구조가 로컬 `map.json` 과 동일했다(레이어 4개·1200칸·`tileIdBase 2049`·`columns 16`)
→ 로컬에서 만들어 그대로 밀어넣으면 된다.

> 세션 시작 시 `/api/me` 가 `{"user":null}` 이었다(A-3 만료).
> ACCOUNT → 「다시 로그인」 으로 복구됨(비밀번호 안 물어봄). **SPUM 작업 전 로그인 상태부터 확인할 것.**

---

## 0-b. 해커톤 세션 2차 후반 — P0(게임 셸) 완료

`proto/game.html` 신규. SPUM Engine 위에서 **부팅 → 타일맵 → 캐릭터 → 이동 → 충돌 → 카메라 추적** 동작.
`index.html`(v1)은 비교용으로 남겨 뒀다(폐기된 "외부 탐정" 기획 화면).

**엔진 소스를 읽고 확인한 사실 (문서가 없어 직접 확인함):**
- `SpumEngine.createEngine(canvas, opts)` → `Engine`. 기본 `targetFPS: 30`.
- `TileSet(img, 12, 12, {firstId: 2049})` — 시트에서 **잘라올 크기(12px)**.
  `TileMapSystem({tileWidth: 32})` — **화면에 그릴 크기(32px)**. 둘은 별개다.
  `firstId` 는 SPUM 테마의 `tileIdBase` 와 같은 값(2049).
- `Camera.isMain = true` 를 켜야 `Engine._getMainCamera()` 가 찾는다.
- 렌더 순서 = `sortingLayer` → `transform.y` → `renderOrder`. 타일맵 -100, 캐릭터 10, 이름표 20.
- ⚠ **`CameraController` 는 추적 카메라가 아니다** — 마우스 줌/팬 전용.
  따라가는 카메라는 `FollowCamera` 로 직접 만들었다(`Camera.clampPosition` 으로 맵 밖 안 보이게).
- ⚠ **캐릭터 초상 PNG 에 알파가 없다(100% 불투명).** 그대로 얹으면 검은 사각형이 깔린다.
  → `tools/cut-chars.mjs` 로 테두리 flood-fill 배경 제거 → `assets/chars/cut/*.png`.
    (색 전역 제거는 안 된다 — 머리카락이 배경만큼 어둡다.)
- ⚠ **스프라이트 고유 개수는 3/5 가 아니라 2/5 였다.**
  꾸벅이=폴짝이=새침이, 오물오물=살살이. 일단 색 원판+이름표로 구분한다. (§5-1 해결책 필요)
- `SpriteRenderer` 사용(엔진 `Character` 는 슬롯 파츠 JSON 이 필요한데 우리에겐 없다). 걷기 애니메이션 없음.

**검증 (★★★★★ 직접 확인):** 탭이 비활성이면 `requestAnimationFrame` 이 멈춰 FPS 0 이 된다(코드 문제 아님).
`engine.pause()` 후 `engine.step(1/60)` 으로 결정론적 검증함 —
이동 정상 · **벽으로 400프레임 밀어붙여도 관통 없음**(방 경계에서 정확히 정지) · 카메라 추적/클램프 정상.

## 0-c. 다음 세션 시작점 — P1

1. **시야 제한** — 내 주변/같은 방만 보이게 (기획의 핵심, `game-spec.md` §1)
2. **근접 대화** — 가까이 가서 말 걸기 → 기존 `/api/ask`(SAM) 재사용
3. **AI 4명 이동** — `Walker` 는 이미 5명 전원에게 붙어 있다(`isPlayer` 만 false). 여기에 AI 조종을 얹으면 된다.

---

## 1. 지금 어디까지 왔나

| 영역 | 상태 |
|---|---|
| **기획** | ✅ 확정 (`game-spec.md`) — 플레이어도 거주자 1명, 어몽어스식. 결정: 범인 랜덤(플레이어 포함) / 밤에 직접 조작 / 토론 후 투표 |
| **밤 시뮬 근거 엔진** | ✅ **검증 7/7** (`proto/run.mjs`) — 거짓말·목격증언·3연타 자백·지어내기 방지 |
| **SAM 대화 연동** | ✅ 작동 (`proto/engine.mjs`, `/v1/generate`, 턴당 SSAM ~1.7) |
| **타일 테마** | ✅ `셰어하우스 인테리어` 173타일 (SPUM Object, AI 생성) |
| **맵** | ✅ `셰어하우스` 40×30 — 방5 + 부엌 + 중앙 거실 (SPUM Map) |
| **캐릭터 5명** | ✅ 성격·기억·역할 주입 + 외형 5인 차별화 (SPUM Cast) |
| **월드** | ✅ 캐스트 5명 배치 + **맵을 셰어하우스로 교체 완료** |
| **게임 런타임** | ✅ 타일맵·이동·충돌·카메라 + NPC 배회 · 근접 대화(맥락 유지) · **게임 루프 전 구간**. 다음은 기술문서 |

## 2. 다음 세션이 할 일 (우선순위)

**P0 — SPUM Engine으로 게임 셸 세우기**
1. `proto/vendor/packages/spum-engine/index.js` 를 import 해서 `SpumEngine.createEngine("canvas")` 부팅
2. `TileMap`으로 `assets/map.json`(40×30) 렌더
3. `Character` 1명 배치 → `InputManager`로 이동 → `CameraController`로 추적
4. `Collider`/walkable 레이어로 벽 충돌
> 문서가 없으므로 **소스를 읽어가며** 붙인다. 최소 부팅부터 확인하고 하나씩 늘릴 것.

**P1 — 플레이어 관점 구현 (기획의 핵심)**
5. 내 캐릭터 지정 + **시야 제한**(내 주변/같은 방만 보임)
6. 근접 대화(가까이 가서 말 걸기) — 기존 SAM 호출 재사용
7. 밤: 플레이어 직접 이동, 범인이면 몰래 부엌行

**P2 — 루프 완성**
8. 저녁 → 밤 → 아침 → 토론 → **투표** → 판정
9. 매 판 범인 랜덤 배정(플레이어 포함) + 밤 시뮬 생성

**P3 — 마감 정리**
10. 기술문서 마무리, 리허설, 커밋

## 3. 자산 위치

```
90_hackathon/01_soonsoon-social-deduction/
├─ game-spec.md          기획/작업정의서 ← 먼저 읽기
├─ work-rules.md         작업 규칙서(함정 모음) ← 반드시 읽기
├─ handoff.md            이 문서
├─ _local/               ⛔비커밋 — 회사 원본·내부자료 (과제정의서 docx 등)
└─ proto/
   ├─ engine.mjs         캐릭터 데이터 + 프롬프트 빌더 + SAM 호출 (재사용)
   ├─ server.mjs         로컬 서버 + SAM 프록시 (키 미노출)
   ├─ run.mjs            자동 검증 시나리오 (7/7)
   ├─ game.html          ★현재 게임 (SPUM Engine)
   ├─ index.html         v1 캔버스 화면 — 폐기된 기획, 비교용으로만 보관
   ├─ tools/             build-map · png · cut-chars · atlas (개발 도구)
   ├─ chat.html          v0 채팅 프로토타입
   ├─ assets/
   │   ├─ map.json               40×30 레이어 + 타일좌표 매핑 (셀 12px)
   │   ├─ tileset_source.png     192×192, 16×16 그리드
   │   └─ chars/*.png            ⚠️ 3/5만 고유(썸네일 캐시 문제)
   └─ vendor/            spum-engine 65파일 (미러링, .gitignore로 커밋 제외)
```

**vendor 복구 방법** (새 환경에서):
```bash
cd proto && node mirror.mjs   # spum.soonsoon.ai/packages/spum-engine 재귀 다운로드
```
※ `mirror.mjs` 는 해커톤 세션 2차에서 **레포에 넣어 뒀다** (`proto/mirror.mjs`). vendor 65파일도 복구됨.

## 4. SPUM 리소스 ID (실계정)

| 항목 | ID |
|---|---|
| 월드 | `WORLD_mr0ou29f_P5XI47` (누가 내 케이크 먹었어?) |
| 맵 | `MAP_msysua8z_CKYO` (셰어하우스 40×30) |
| 타일 테마 | `SMO_msysgdda_X079` (셰어하우스 인테리어, 173타일) |
| 캐릭터 | 오물오물 `CHAR_msyjmqx3_UOXG` · 새침이 `CHAR_msyjkgad_61WB` · 살살이 `CHAR_msyjmqx4_HK5E` · 꾸벅이 `CHAR_msyjmqx4_258C` · 폴짝이 `CHAR_msyjmqx4_BSQ0` |

**방 좌표(맵 기준)**: 오물오물 `6,5` · 폴짝이 `19,4` · 새침이 `33,5` · 살살이 `6,24` · 꾸벅이 `33,24` · 거실 `19,14` · 부엌 `19,25`

## 5. 알려진 이슈

1. **캐릭터 스프라이트 2/5만 고유** (해커톤 세션 2차에서 재확인 — 3/5가 아니었다) — Cast Export가 직전 캐릭터 시트를 내려받고(A-4), 썸네일은 외형 수정이 반영 안 됨(A-5). 해결책: Cast Editor에서 각 캐릭터를 UI로 한 번씩 편집→저장해 썸네일 재생성 후 추출.
2. **타일 원본이 192×192(셀 12px)** — 확대 시 거칠다. 필요하면 Object Editor에서 고해상도 재생성.
3. **월드 캐스트 spawn 좌표가 옛 맵 기준** — 코드로 바꿔도 앱이 되돌린다(A-2). World Editor에서 드래그로 옮길 것.
4. **SPUM 세션 30분 만료** — 자동 복구 절차는 `work-rules.md` A-3.

## 6. 계정/비용

- SPUM/SAM 계정: 팀 공용 계정 (이메일·플랜 정보는 `_local/` 참조 — 공개 레포에 적지 않는다)
- SSAM 잔량: 약 **44,220 / 45,000** (이번 달 $0.70 사용) — 넉넉함
- SAM MCP(`/mcp`)는 **연결 성공했으나 SPUM 조작 툴이 아님**(웹검색·페이지열기·사용량). 게임과 무관 — 더 파지 말 것.
