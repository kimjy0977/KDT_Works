# 구현 계획
## 시작 조건
D01~D07의 최초 범위와 계약·완료 조건을 정하고 학생이 구현을 요청했다. D08/D09는 미뤄도 된다.

## 1. 첫 수직 구현
사용자 요청 → 실제 모델 → 읽기 도구 → 모델에 실행 결과 전달 → 결과 표시를 가장 작은 입력으로 잇는다. 수직 구현은 UI부터 저장·도구 등 필요한 층을 지나 사용자 결과 하나까지 끝내는 작업이다. 모든 화면/도구를 먼저 만들지 않는다.
- 선택 작업/도구: `work/meeting.txt` 읽고 시각·준비물 답하기 · `read_file`
- 필요한 파일·의존성과 실행 방법: [AI가 선택 언어에 맞춰 작성]
- 실제 모델 사용 전 모의 응답으로 확인할 부분: 도구 요청↔결과 연결 · 잘못된 인수 · 한도 · 승인 거절 (`uv run python -m pytest -q` — **66건 통과**)
- 완료 증거: 세션 `local-reading2.json` · 실행 기록 `.harness/runs/*.jsonl` · `model_calls 2 · tool_calls 1 · tool_errors 0 · 11.1초` **(A01)**

## 2. 범용 작업의 품질
원문과 결과 대조, 인자 오류, 경로 이탈, 반복 한도를 검증한다. 실패를 보고 계약/코드를 수정한다.

## 3. 코딩 작업과 권한
실습용 작은 결함 코드/테스트를 준비한다. 변경 제안 → 내용 표시 → 승인/거절 → 적용 → 지정 테스트 → 결과를 연결한다. 거절 시 파일 불변도 확인한다.

## 4. 제품 기능
선택한 세션 정책과 상태 표시를 구현한다. 추가 provider, 영속 저장, 취소 등은 DECISIONS의 범위에 따라 작업을 추가하거나 후속으로 남긴다.

## 5. 고정 10문항 비교 실험
harness-lab의 benchmark/tasks.json과 로컬 평가 연결 방식을 확인한다. 기준 실행 후 실패 기록을 바탕으로 가설 하나를 선택하고 개선한 뒤 같은 10문항을 재측정한다. 모델·예산·반복 수·원본 버전을 유지하고 차이가 있으면 기록한다. 원본 결과·CSV·HTML과 구현 소스를 함께 제출한다. 채점기 진단과 모의 검사는 학생 성능 점수가 아니다.

## 각 단계의 기록
| 단계 | 관련 요구·검증 ID | 구현할 변경 | 확인한 결과 | 남은 문제 |
|---|---|---|---|---|
| 0. 제공 구현 읽기 | 9강 | 없음(읽기) | `harness-lab` v4.0.0 설치·실행. A01~A08 통과 | 이 증거는 **제공 구현**의 것이다 |
| 1. 내 하네스 골격 | R01·R02·D03·D09 | `myharness/core.py` — 자료형·Workspace·Tools·OllamaProvider·Agent. **표준 라이브러리만** | 모델→도구→결과→모델 반복이 **내 코드에서** 돈다 | 토큰 사용량 미수집(`usage_known=False`) |
| 2. CLI와 세션 | D02·D07 | `run.py` — 세션 파일 저장, 임시파일 후 **교체**, 재개 시 조건 불일치면 새 이름 요구 | 세션·실행기록 분리 저장 확인 | 다중 프로세스 동시 사용 미지원 |
| 3. 경로 검사 | R05·A05 | `Workspace.resolve` — `..`·숨김·심볼릭·폴더 밖 절대경로 거부 + 이식 루트 매핑 | 탈출 4종 전부 거부 확인 | OS 격리 아님(명시) |
| 4. ★첫 실행에서 버그 2건 발견 | A07 | ①도구 오류에 **이유·인자**를 기록 ②최종 텍스트가 비면 `completed`가 아니라 **`empty_answer`** | 기록에서 `/app/2026-09-01-회의.md` 요청이 **보이게** 됨 | — |
| 5. ★시스템 프롬프트 버그 | R03 | CLI에도 "/app·/protected가 옮겨진다"고 써서 **모델이 없는 경로를 지어냈다**. 모드별로 분리 | 도구 오류 2 → **0** | 설명이 곧 «있다»는 신호가 된다는 교훈 |
| 6. 통제 시나리오 | D01 | 없음(검증) | 「담당자 미정」 항목을 **지어내지 않고** 미정으로 분류 | 서술 품질은 낮음(2.3B) |

설계 변경은 PRD와 인터페이스, 완료 조건에도 반영한다. 코드가 먼저 바뀌어 명세와 충돌했다면 어느 쪽이 사용자 의도에 맞는지 판단한다.
